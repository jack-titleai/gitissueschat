#See README.md for instructions on setting up environment variables

# GitHub Personal Access Token (Required for accessing GitHub repositories)
# Create one at https://github.com/settings/tokens with the following scopes:
# - repo (Full control of private repositories)
# - read:org (Read-only access to organization membership)
GITHUB_TOKEN="your_github_personal_access_token"

# Google Cloud Project ID (Required for Google Vertex AI embeddings)
# Find this in your Google Cloud Console
GOOGLE_PROJECT_ID="your_google_project_id"

# Path to Google Cloud Service Account Credentials (Required for Google Vertex AI)
# This should be the absolute path to your JSON credentials file
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/google_credentials.json"
